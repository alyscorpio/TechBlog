<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
        <link rel="stylesheet" type="text/css" href="/css/style.css">
        <title>The Tech Blog</title>
    </head>

    <body>

        <header>
            <h1 class="title px-3 py-2 bg-dark text-white">The Tech Blog</h1>
            <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <div class="container-fluid">
                <a class="navbar" href="/">Home</a>
                <a class="navbar" href="/dashboard">Dashboard</a>
            {{#if logged_in}}
                <button class="navbar btn" id="logout">Logout</button>
            {{else}}
                <a class="navbar" id="login" href="/login">Login</a>
            {{/if}}
            </div>
            </nav>
        </header>

        <main>
            {{{ body }}}
        </main>

        <footer>
            Site Created by Alys Dickerson
        </footer>

        {{#if logged_in}}
        <script src="/js/logout.js"></script>
        {{/if}}

    </body>

</html>